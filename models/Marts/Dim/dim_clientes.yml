version: 2

models:
  - name: dim_clientes
    description: Tabela de informações de clientes
    columns:
      - name: id
        description: Chave primária da tabela de clientes.
        tests:
          - unique
          - not_null
