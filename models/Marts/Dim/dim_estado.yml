version: 2

models:
  - name: dim_estado
    description: Dimensão que contém informações sobre estados na base de dados Northwind.
    columns:
      - name: province_id
        description: Chave primária da dimensão estado.
        tests:
          - unique
          - not_null

          
