version: 2

models:
  - name: dim_produto
    description: Tabela de detalhes dos pedidos de venda.
    columns:
      - name: order_id
        description: Chave primária da tabela de detalhes dos pedidos de venda.
        tests:
          - not_null
     
