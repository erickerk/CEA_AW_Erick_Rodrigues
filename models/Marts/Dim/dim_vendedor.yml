version: 2

models:
  - name: dim_vendedor
    description: Tabela de informações de pessoas.
    columns:
      - name: person_business_entity_id
        description: Chave primária da tabela de pessoas.
        tests:
          - unique
          - not_null
